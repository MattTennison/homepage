version: "3.8"
services:
  image-api:
    build:
      context: ./image-api
    ports:
      - 3000:3000
    env_file: ./image-api/.env
    volumes:
      - ./image-api:/app
      - /app/node_modules
    command: yarn start:dev
